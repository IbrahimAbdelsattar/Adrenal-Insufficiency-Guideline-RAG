# Clinical Decision Support Lite — backend dependencies
# Feature 001-clinical-rag-ingestion

# --- Web framework ---
fastapi>=0.115
uvicorn[standard]>=0.32

# --- Configuration ---
pydantic>=2.9
pydantic-settings>=2.6
python-dotenv>=1.0

# --- Ingestion ---
pymupdf>=1.24          # PDF parsing (research.md D4). AGPL-3.0.
tiktoken>=0.8          # cl100k_base token counting (research.md D6)
pyyaml>=6.0            # sources.yaml provenance registry

# --- Vector store & embeddings ---
chromadb>=0.5          # Embedded persistent vector store (research.md D2)
httpx>=0.27            # OpenRouter embeddings client (research.md D3)

# --- Tests ---
pytest>=8.3
pytest-asyncio>=0.24
